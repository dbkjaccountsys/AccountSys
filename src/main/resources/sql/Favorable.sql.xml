<?xml version="1.0" encoding="UTF-8" ?>
<sql-statement namespace="com.dbkj.account.model.Favorable">
    <sql id="getPage">
        <![CDATA[
            SELECT t2.* FROM
            (
                SELECT id FROM favorable WHERE `status`=1 AND endtime>=NOW() LIMIT ?,?
            ) t1
            INNER JOIN favorable t2 ON t1.id=t2.id ORDER BY t2.id DESC;
        ]]>
    </sql>
    <sql id="getCount">
        SELECT COUNT(1) AS `count` FROM favorable
    </sql>
</sql-statement>